import { StyleSheet } from "react-native";
export const authStyle = StyleSheet.create({
  main: {
    flex: 1,
    padding: 20,
  },
  title: {
    fontSize: 20,
    color: "black",
    fontFamily: "mt-bold",
  },
});
